<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= @band.name %> | Apollo</title>
  <link rel="icon" type="image/png" href="{{Favicon}}" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <!-- Social meta tags -->
  <meta name="description" content="<%= @band.get_field_value(@music_desc_option) %>" />
  <meta property="og:title" content="<%= @band.name %> | Apollo" />
  <meta property="og:type" content="profile" />
  <meta property="og:url" content="{{url}}" />
  <meta property="og:description" content="<%= @band.get_field_value(@music_desc_option) %>" />

    <!--
    <%= @band.get_field_value(@text_color_font_option) %>
    <%= @band.get_field_value(@main_color_font_option) %>
      -->

  <%= stylesheet_link_tag  "themes/#{ @band.theme.identifier }/main", media: "all" %>
  <%= csrf_meta_tags %>

  <style>

    html, body{
      color: <%= @band.get_field_value(@main_color_field_option) %>;
    }

    a{
      color: <%= @band.get_field_value(@main_color_field_option) %>;
    }
    <% image_id = @band.get_field_value(@background_image_field_option) || 1 %>
    #site_wrap{
      background-image: url("/assets/background_images/<%= "%.2d" % image_id %>.jpg");
    }
    #overlay_color{
      background-color: <%= @band.get_field_value(@background_overlay_color_field_option) %>;
      opacity: <%= @band.get_field_value(@background_overlay_opacity_field_option).to_f / 100 %>;
    }

    .music_section,
    .music_section .album,
    #mobile_header,
    #mobile_menu{
      background-color: <%= @band.get_field_value(@music_background_color_field_option) %>;
    }
    .music_section .album .album_info_wrapper .album_info .album_button{
      color: <%= @band.get_field_value(@music_background_color_field_option) %>;
      background-color: <%= @band.get_field_value(@text_color_field_option) %>;
    }

    .music_section .album .album_info_wrapper .album_info .album_description,
    .music_section .album .album_info_wrapper .album_info .album_title,
    .music_section .album .album_info_wrapper .album_info .album_type{
      color: <%= @band.get_field_value(@text_color_field_option) %>;
    }


  </style>

</head>
<body>
  <!--[if lt IE 9]>
    <div id="ie_upgrade_message">
      <p>This website is not supported on your browser. It uses up-to-date technology that this version of Internet Exploer does not support.</p>
      <p>Please <a href="http://browsehappy.com/">upgrade to a modern browser</a> for the best viewing experience possible.</p>
    </div>
  <![endif]-->

  <%= yield %>

  <%= javascript_include_tag "themes/#{ @band.theme.identifier }/theme" %>

</body>
</html>